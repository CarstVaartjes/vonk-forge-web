[build]
builder = "DOCKERFILE"
dockerfilePath = "api/Dockerfile"

[deploy]
startCommand = "uvicorn vonk_catalog.runtime:create_app --factory --host 0.0.0.0 --port $PORT --no-access-log"
healthcheckPath = "/health/ready"
restartPolicyType = "ON_FAILURE"
